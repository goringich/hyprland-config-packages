<h1 align="center"><span style="color:#7aa2f7;">arch-dotfiles</span></h1>
<p align="center">
  <span style="color:#a6da95;">Полная копия моего рабочего окружения Hyprland ➜ готова к deploy на свежий CachyOS.</span><br/>
  <span style="color:#f4dbd6;">Hyprland · Waybar · Rofi · Kitty · AGS · Kvantum · GTK · Fastfetch и многое другое</span>
</p>

---

## 🌈 Что внутри
- <span style="color:#eed49f;">`.config/`</span> — зеркальная копия ключевых конфигов (Hyprland, Waybar, Rofi, Kitty, AGS, Kvantum, GTK, Fastfetch, SwayNC, Wlogout, Wallust, Btop/Bpytop, Cava и др.).
- <span style="color:#eed49f;">`pkglist.txt`</span> — список всех явно установленных пакетов `pacman`.
- <span style="color:#eed49f;">`bin/sync-dotfiles.sh`</span> — помощник для обновления репозитория из текущей системы.

> 💡 Повторный запуск безопасен: все копирования используют `rsync --delete`, поэтому репозиторий всегда соответствует текущим настройкам.

## 🚀 Быстрый старт на новом CachyOS

```bash
# 1. Установи git и rsync, если их нет
sudo pacman -Syu --needed git rsync

# 2. Клонируй репозиторий в домашнюю директорию (пример)
git clone git@github.com:goringich/hyprland-config-packages.git ~/arch-dotfiles
cd ~/arch-dotfiles

# 3. Создай бэкап текущих конфигов (если система уже настроена)
mkdir -p ~/config-backup && rsync -a ~/.config/ ~/config-backup/

# 4. Скопируй свежие конфиги из репозитория в систему
rsync -a --delete .config/ ~/.config/

# 5. Установи все пакеты из списка
sudo pacman -Syu --needed - < pkglist.txt

# 6. Перезапусти графическую сессию Hyprland
hyprctl dispatch exit 0
# затем выполни вход снова — рабочий стол загрузится с этими настройками
```

### 🛡️ Если нужно применить выборочно
```bash
# пример для Waybar
rsync -a --delete .config/waybar/ ~/.config/waybar/
```

## 🔄 Как обновлять репозиторий
```bash
# из каталога ~/arch-dotfiles
./bin/sync-dotfiles.sh
# скрипт синхронизирует ~/.config → ./dotfiles и обновит pkglist.txt
```

## 🌐 Публикация на GitHub
```bash
# находясь в ~/arch-dotfiles
git remote add origin git@github.com:<your-user>/arch-dotfiles.git
# или использовать HTTPS-адрес
# git remote add origin https://github.com/<your-user>/arch-dotfiles.git

git branch -M main
git push -u origin main
```

> 🔑 Не забудь добавить SSH-ключ или выполнить `gh auth login`, если используешь HTTPS+

## 🧭 Полезные заметки
- После восстановления можно запустить `hyprctl reload`, чтобы перечитать конфиги без выхода из сессии.
- Для GTK/Kvantum тем может потребоваться повторный вход, чтобы темы подтянулись во все приложения.
- CachyOS совместим с Arch-пакетами, но если какой-то пакет отсутствует, проверь AUR/Chaotic-AUR.

<p align="center">
  <span style="color:#c6a0f6;">Happy rice! 🌸</span>
</p>
